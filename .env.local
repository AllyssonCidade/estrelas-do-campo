
# Frontend Environment Variables (used by Next.js)
# These are prefixed with NEXT_PUBLIC_ to be exposed to the browser.
# Replace with your actual Vercel deployment URLs or localhost for local dev.
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_FRONTEND_URL=http://localhost:9002

# Backend Admin Password (exposed client-side for admin login check - less secure)
ADMIN_PASSWORD=estrelas123

# Supabase variables (if switching fully to Supabase REST API later)
# NEXT_PUBLIC_SUPABASE_URL=YOUR_SUPABASE_PROJECT_URL
# NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
